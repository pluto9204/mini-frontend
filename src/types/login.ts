export interface LoginCredentials {
    id: string;
    password: string;
}

export interface JoinCredentials {
    id: string;
    password: string;
    username: string;
    email: string;
    phonenumber: string;
}